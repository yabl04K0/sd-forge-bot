# ЧЕК — отложено

<!--
AI напоминает об этих записях в футере каждого прогона.
Если код вокруг символа изменился относительно deferred_sha → запись поднимается в chek_open.md.
Формат:

- id: "file::symbol::pattern"
  reason: "почему отложено"
  deferred_sha: "<git sha кода вокруг символа на момент откладывания>"
-->

- id: "bot.py::generate_image::markdown-status-outside-try"
  reason: "Статус-сообщение (format_params с промптом в бэктиках) отправляется до try; бэктик в промпте ломает Markdown-парсинг и роняет хендлер без обработки. Низкая вероятность (бэктики в SD-промптах редки), требует аккуратной экранизации/ParseMode-стратегии — отложено."
  deferred_sha: "c154f28"

- id: "database.py::update_user_setting::sql-key-fstring"
  reason: "Имя колонки key подставляется в SQL через f-string. Сейчас НЕ эксплуатируется — все вызывающие передают литеральные ключи. Фикс (whitelist колонок) защитный, не срочный — отложено."
  deferred_sha: "c154f28"
